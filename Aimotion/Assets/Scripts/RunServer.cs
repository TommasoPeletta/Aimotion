using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Diagnostics;
using AsyncIO;
using NetMQ;
using NetMQ.Sockets;

public class RunServer : MonoBehaviour
{
    // Start is called before the first frame update
    /*
    void Start()
    {
        UnityEngine.Debug.Log(Application.streamingAssetsPath);
        string path = Application.streamingAssetsPath + "/aim_heart.py";
        Process foo = new Process();
        //foo.StartInfo.UseShellExecute = true;
        foo.StartInfo.FileName = path;
        foo.Start();
    }*/

    // Update is called once per frame
    void Update()
    {
        
    }
}
